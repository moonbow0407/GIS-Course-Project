# 图层右键"打开文件夹"实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 图层面板右键菜单新增"打开文件夹"项，打开图层数据文件所在目录；无本地文件的图层点击后状态栏提示。

**Architecture:** 沿用现有"控件发信号、主窗口处理"模式——`LayerPanel` 新增 `layer_folder_requested(str)` 信号与菜单项，`MainWindow._open_layer_folder` 从 `snapshot()` 读取 `source_path` 并用 `QDesktopServices.openUrl` 打开父目录。

**Tech Stack:** PySide6（QDesktopServices/QUrl）、pytest + offscreen 测试模式、ruff、mypy。

## Global Constraints

- 领域层（`app/domain/`、`app/application/`）不做任何修改——`source_path` 已存在于 `VectorLayer`/`RasterLayer`，`LayerSnapshot.layer` 可直接读取。
- 菜单项始终显示、始终可点击（用户已确认），无本地文件时由主窗口状态栏提示"图层 {name} 没有本地数据文件"。
- 不做 `explorer /select` 选中高亮；不做数据库图层的"打开数据库"入口；不缓存路径到树节点。
- 无撤销/重做（纯系统操作）。
- 中文文档字符串、类型注解、最小改动（AGENTS.md）。
- 测试必须用 `QT_QPA_PLATFORM=offscreen` 并在创建 Qt 控件前 `QApplication.instance() or QApplication([])`。
- 所有 pytest 命令用 `uv run pytest`；Qt 测试失败输出可能被吞掉时先加 `-q` 确认。

---

### Task 1: LayerPanel 新增信号与"打开文件夹"菜单项

**Files:**
- Modify: `app/presentation/widgets/layer_panel.py`（信号声明区 + `_on_context_menu_requested`）
- Test: `tests/presentation/test_layer_open_folder.py`（新建，本任务写入辅助函数与第一个测试）

**Interfaces:**
- Produces: 信号 `LayerPanel.layer_folder_requested = Signal(str)`（携带图层编号）；右键菜单第 4 项（"打开属性表"之后）为"打开文件夹"，选中时发出该信号。后续任务依赖此信号名。

- [ ] **Step 1: 新建测试文件并写失败测试**

创建 `tests/presentation/test_layer_open_folder.py`：

```python
"""图层右键"打开文件夹"菜单与打开行为测试。"""

import os
from pathlib import Path

# 必须在导入 Qt 前启用无界面平台，测试才能在没有显示器的环境运行。
os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")

from pyproj import CRS
from PySide6.QtCore import QPoint
from PySide6.QtGui import QAction
from PySide6.QtWidgets import QApplication, QMenu, QTreeWidgetItem

from app.application.results import LayerSnapshot, WorkspaceSnapshot
from app.domain.feature import Feature
from app.domain.vector_layer import VectorLayer
from app.presentation.widgets.layer_panel import LayerPanel
from shapely.geometry import Point


def _make_layer(layer_id: str, name: str, source_path: Path | None) -> VectorLayer:
    """构造带单个点要素和可选数据文件路径的测试矢量图层。"""
    return VectorLayer.create(
        layer_id=layer_id,
        name=name,
        features=(Feature(fid=1, geometry=Point(0, 0), attributes={}),),
        crs=CRS.from_epsg(4549),
        source_path=source_path,
    )


def _snapshot(layer: VectorLayer) -> WorkspaceSnapshot:
    """构造只含一个图层的完整工作区快照。"""
    return WorkspaceSnapshot(
        layers=(LayerSnapshot(layer=layer, visible=True, selected_feature_ids=()),),
        active_layer_id=None,
        display_crs=layer.crs,
    )


def test_context_menu_contains_open_folder_and_emits_layer_id(monkeypatch) -> None:
    """右键菜单应包含"打开文件夹"项，选择后发出携带图层编号的信号。"""
    QApplication.instance() or QApplication([])
    panel: LayerPanel = LayerPanel()
    panel.apply_snapshot(
        _snapshot(_make_layer("a", "图层A", Path("D:/data/roads.geojson")))
    )
    panel.resize(400, 300)
    panel.show()
    QApplication.processEvents()
    item: QTreeWidgetItem | None = panel._tree.topLevelItem(0)
    assert item is not None

    menu_texts: list[str] = []
    emitted: list[str] = []
    panel.layer_folder_requested.connect(emitted.append)

    def execute(menu: QMenu, position: QPoint) -> QAction | None:
        menu_texts.extend(action.text() for action in menu.actions())
        return menu.actions()[3]

    monkeypatch.setattr(panel, "_execute_context_menu", execute)
    rect = panel._tree.visualItemRect(item)
    panel._on_context_menu_requested(rect.topLeft() + QPoint(5, 5))

    assert menu_texts == [
        "缩放至图层",
        "符号系统",
        "打开属性表",
        "打开文件夹",
        "删除图层",
    ]
    assert emitted == ["a"]
    panel.close()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/presentation/test_layer_open_folder.py -q`
Expected: FAIL——`IndexError`（菜单只有 4 项，`menu.actions()[3]` 越界）或断言失败。

- [ ] **Step 3: 实现菜单项与信号**

在 `app/presentation/widgets/layer_panel.py` 中：

信号声明区（第 96 行 `layer_removed` 声明之后）添加：

```python
    # 图层文件夹请求信号：请求打开指定图层数据文件所在文件夹。
    layer_folder_requested = Signal(str)
```

`_on_context_menu_requested`（约第 266-270 行）在"打开属性表"与"删除图层"之间插入菜单项，并添加分支：

```python
        menu: QMenu = QMenu(self)
        zoom_action = menu.addAction("缩放至图层")
        symbology_action = menu.addAction("符号系统")
        attribute_action = menu.addAction("打开属性表")
        open_folder_action = menu.addAction("打开文件夹")
        remove_action = menu.addAction("删除图层")
        selected_action: object | None = self._execute_context_menu(menu, position)
        layer_id: str = str(item.data(0, Qt.ItemDataRole.UserRole))
        if selected_action is zoom_action:
            self.layer_zoom_requested.emit(layer_id)
        elif selected_action is symbology_action:
            self.layer_symbology_requested.emit(layer_id)
        elif selected_action is attribute_action:
            self.layer_attribute_requested.emit(layer_id)
        elif selected_action is open_folder_action:
            self.layer_folder_requested.emit(layer_id)
        elif selected_action is remove_action:
            self.layer_removed.emit(layer_id)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/presentation/test_layer_open_folder.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add tests/presentation/test_layer_open_folder.py app/presentation/widgets/layer_panel.py
git commit -m "feat: 图层右键菜单新增打开文件夹入口"
```

---

### Task 2: MainWindow 打开文件夹处理与信号接线

**Files:**
- Modify: `app/presentation/main_window.py`（imports、`_connect_signals`、新增 `_open_layer_folder` 方法）
- Test: `tests/presentation/test_layer_open_folder.py`（追加三个测试）

**Interfaces:**
- Consumes: `LayerPanel.layer_folder_requested = Signal(str)`（Task 1 产出）
- Produces: `MainWindow._open_layer_folder(layer_id: str) -> None`——有路径时用 `QDesktopServices.openUrl(QUrl.fromLocalFile(str(source_path.parent)))`，无路径时 `self.statusBar().showMessage(f"图层“{layer.name}”没有本地数据文件。")`，图层不存在静默返回。

- [ ] **Step 1: 追加失败测试**

在 `tests/presentation/test_layer_open_folder.py` 末尾追加（同时补 import：`from types import SimpleNamespace`；`from PySide6.QtCore import QUrl`；`from app.application.gis_application import GisApplication`；`from app.domain.map_document import MapDocument`；`from app.infrastructure.file_io.auto_reader import AutoDataReader`；`from app.infrastructure.file_io.auto_writer import AutoDataWriter`；`from app.presentation.main_window import MainWindow`）：

```python
def _make_window(layer: VectorLayer) -> MainWindow:
    """创建主窗口并替换为使用指定图层的应用服务。"""
    QApplication.instance() or QApplication([])
    document: MapDocument = MapDocument()
    document.add_layer(layer)
    window: MainWindow = MainWindow()
    window._application = GisApplication(AutoDataReader(), AutoDataWriter(), document)
    window._refresh_workspace()
    return window


def test_open_layer_folder_opens_parent_directory(monkeypatch) -> None:
    """有数据文件的图层应打开其所在父目录。"""
    source: Path = Path("D:/data/roads.geojson")
    window: MainWindow = _make_window(_make_layer("a", "图层A", source))
    opened: list[QUrl] = []
    monkeypatch.setattr(
        "app.presentation.main_window.QDesktopServices",
        SimpleNamespace(openUrl=lambda url: opened.append(url) or True),
    )

    window._open_layer_folder("a")

    assert opened == [QUrl.fromLocalFile(str(source.parent))]
    window.close()


def test_open_layer_folder_reports_missing_local_file(monkeypatch) -> None:
    """没有本地数据文件的图层应提示而不是打开文件夹。"""
    window: MainWindow = _make_window(_make_layer("a", "图层A", None))
    opened: list[QUrl] = []
    monkeypatch.setattr(
        "app.presentation.main_window.QDesktopServices",
        SimpleNamespace(openUrl=lambda url: opened.append(url) or True),
    )

    window._open_layer_folder("a")

    assert opened == []
    assert "没有本地数据文件" in window.statusBar().currentMessage()
    window.close()


def test_layer_folder_requested_signal_is_wired(monkeypatch) -> None:
    """图层面板打开文件夹请求应连接到主窗口处理。"""
    source: Path = Path("D:/data/roads.geojson")
    window: MainWindow = _make_window(_make_layer("a", "图层A", source))
    opened: list[QUrl] = []
    monkeypatch.setattr(
        "app.presentation.main_window.QDesktopServices",
        SimpleNamespace(openUrl=lambda url: opened.append(url) or True),
    )

    window._layer_panel.layer_folder_requested.emit("a")

    assert opened == [QUrl.fromLocalFile(str(source.parent))]
    window.close()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/presentation/test_layer_open_folder.py -q`
Expected: 三个新测试 FAIL（`AttributeError: ... has no attribute '_open_layer_folder'` 或 `AttributeError: module ... has no attribute 'QDesktopServices'`），Task 1 的测试仍 PASS。

- [ ] **Step 3: 实现主窗口处理**

`app/presentation/main_window.py` 中：

导入修改（第 11-12 行）：

```python
from PySide6.QtCore import QPointF, QUrl, Qt, QTimer
from PySide6.QtGui import QCloseEvent, QDesktopServices, QKeySequence, QShortcut
```

`_connect_signals` 中 `layer_removed` 连接之后（约第 221 行）添加：

```python
        self._layer_panel.layer_folder_requested.connect(self._open_layer_folder)
```

新增方法（放在 `_remove_layer` 附近，保持图层面板相关操作聚集）：

```python
    def _open_layer_folder(self, layer_id: str) -> None:
        """在操作系统中打开图层数据文件所在文件夹。

        参数:
            layer_id: 待定位数据文件的图层编号。

        说明:
            数据库图层和未导出的数字化临时图层没有本地数据文件，
            此时在状态栏提示而不是打开任何文件夹。
        """
        for layer in self._application.snapshot().layers:
            if layer.layer_id != layer_id:
                continue
            source_path: Path | None = layer.layer.source_path
            if source_path is None:
                self.statusBar().showMessage(f"图层“{layer.name}”没有本地数据文件。")
                return
            QDesktopServices.openUrl(QUrl.fromLocalFile(str(source_path.parent)))
            return
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/presentation/test_layer_open_folder.py -q`
Expected: 4 个测试全部 PASS

- [ ] **Step 5: 静态检查**

Run: `uv run ruff check app tests && uv run mypy app`
Expected: ruff "All checks passed!"；mypy "Success: no issues found in ... source files"

- [ ] **Step 6: 提交**

```bash
git add tests/presentation/test_layer_open_folder.py app/presentation/main_window.py
git commit -m "feat: 图层打开文件夹时定位数据文件所在目录"
```

---

### Task 3: 全量回归验证

**Files:** 无代码改动。

- [ ] **Step 1: presentation 全量回归**

Run: `uv run pytest tests/presentation -q`
Expected: 全部 PASS（含撤销/重做、批量导入、图层生命周期等既有测试）。

- [ ] **Step 2: 全量测试与检查**

Run: `uv run pytest tests -q`
Expected: 除既有的 2 个 rasterio 环境失败（PROJ_LIB 指向 PostGIS 旧版 proj.db，会话前已存在）外全部 PASS。

Run: `uv run ruff check app tests && uv run mypy app`
Expected: 全部通过。

- [ ] **Step 3: 提交**

```bash
git add -u
git commit -m "test: 图层打开文件夹功能回归验证" || true
```

若工作区无其他改动则跳过提交。
